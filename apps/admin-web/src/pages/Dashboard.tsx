/**
 * Owner dashboard — shows live order summary and key metrics.
 * Currently a placeholder; will be wired to /orders API.
 */

export default function Dashboard() {
  return (
    <section>
      <h2 style={{ fontSize: "1.1rem", marginBottom: 12 }}>Owner Dashboard</h2>
      <p style={{ color: "#64748b" }}>Live orders, sales today, queue overview.</p>
    </section>
  );
}
